---
export type TagType = "maintainer" | "creator";
export type TagColor = "orange" | "blue";

interface Props {
  type: TagType;
  color: TagColor;
}

const { type, color } = Astro.props;
const capitalizedType = type.charAt(0).toUpperCase() + type.slice(1);

const bgClass =
  color === "orange" ? "bg-orange-300/30" : color === "blue" ? "bg-blue-300/30" : "bg-gray-300/30";

const textClass =
  color === "orange" ? "text-orange-300" : color === "blue" ? "text-blue-300" : "text-gray-300";
---

<div class=`${bgClass} w-fit px-1 p-0.5 rounded-md`>
  <span class=`${textClass}`>{capitalizedType}</span>
</div>
